<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Business">
<select id="login" resultType="boolean">select count(*) from BusinessMember where
		businessId= #{businessId} and pw= #{pw}
	</select>
	<!-- 폰번호로 아이디값 가져오는 쿼리문 -->
	<select id="getIdByPhone" resultType="String">
	select businessId from BusinessMember where phone = #{phone}
	</select>
	
	<select id="isMember" resultType="boolean">
		select count(*) from BusinessMember where ${key} = #{value}
	</select>
		
	<select id="phoneCheck" resultType="boolean">
		select count(*) from BusinessMember where phone = #{phone}
	</select>
	
	<insert id="insert">
	insert into BusinessMember values(#{code},#{businessId},#{pw},#{name},#{companyName},#{birthDate},#{phone},#{eMail},#{zipcode},#{address1},#{address2},#{agree},#{authGradeCode},sysdate,null,null)
		<selectKey keyProperty="code" resultType="int" order="BEFORE">
			select business_seq.nextval from dual
		</selectKey>
	</insert>
	
	<insert id="totalInsert">
		insert into TotalMember values(total_member_seq.nextval, 0, ${businessmember}, 1003)
	</insert>
	
	<update id="updatePw">
		update BusinessMember set pw = #{pw} where businessId = #{businessId}
	</update>
	
	<select id="selectBusinessMemberInfo" resultType="cc.spring.dto.BusinessMemberDTO">
		select * from businessmember where businessid = #{id}
	</select>
</mapper>