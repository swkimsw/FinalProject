<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<mapper namespace="Data">
	<select id="clientUserList" resultType="cc.spring.dto.MemberDTO>">
	SELECT * FROM MEMBER b join authgrade ag on b.authgradecode= ag.code where b.authgradecode=1003;
	</select>
	<select id="businessUserList" resultType="cc.spring.dto.MemberDTO>">
	SELECT * FROM MEMBER b join authgrade ag on b.authgradecode= ag.code where b.authgradecode=1002;
	</select>
	
	</mapper>