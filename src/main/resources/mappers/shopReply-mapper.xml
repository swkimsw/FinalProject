<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ShopReply">
	
	<select id="selectShopReply" resultType="cc.spring.dto.ShopReplyAskDTO">
		select g.*, c.nickname from groupbuyingask g join clientmember c on g.clientcode = c.code where postcode=${code}
	</select>
	
	<insert id="insertReplyAsk">
		insert into groupbuyingask values(groupbuying_ask_seq.nextval,${postCode},${clientCode},#{content},sysdate,null,null)
	</insert>
	
	<update id="updateReplyAsk">
		update groupbuyingask set content=#{content} where code=${code}
	</update>
	
</mapper>